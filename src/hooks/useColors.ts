import { commonColors } from '@/styles/colors.ts';

const useColors = () => {
  // const colorTheme = useColorScheme();
  // return colorTheme ? darkTheme : lightTheme;

  return commonColors;
};

export default useColors;
